import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Check Your Email | EduGPT",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
